#!/usr/bin/env ruby
$LOAD_PATH << File.join(File.dirname(__FILE__), '..', 'lib')

require 'baransa'
require 'rack'

class Proxy
  def self.stop
    puts "Terminating ProxyServer"
    EventMachine.stop
  end
end

# Start proxy
Baransa::Server.run
